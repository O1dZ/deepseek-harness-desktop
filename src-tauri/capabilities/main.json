{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "local-shell",
  "description": "Permissions for the local loading and desktop settings pages only.",
  "local": true,
  "windows": ["main", "desktop-settings"],
  "permissions": ["core:default"]
}
